#include <stdio.h>
int main () {
    char nome[50];
    float ganho, gasto, saldo;

    printf("Digite o nome da pessoa:");
    scanf("%s", nome);

    printf("Digite a quantidade de ganhos dessa pessoa:");
    scanf("%f", &ganho);

    printf ("Digite a quantidade de gastos dessa pessoa:");
    scanf ("%f", &gasto);
    
    saldo = ganho - gasto;
    
    printf ("O saldo de %s é:R$ %.2f\n", nome, saldo);
    if (saldo>2000){
    printf (" portanto o saldo está alto");
    }else{
    printf (" portanto o saldo está baixo");
    }
    return 0;
}
